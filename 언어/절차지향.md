## 개념
절차를 중심으로 프로그램을 구성하여, 순차적으로 명령을 실행하면서 문제를 해결하는 방식입니다.\
프로그램이 **입력 -> 처리 -> 출력** 형태로 진행됩니다.

대표적으로 C, Fortran, Pascal 등의 언어가 있습니다.

## 특징
- 코드가 위에서 아래로 순차적으로 실행됩니다.
- 함수를 중심으로 동작합니다.
- 상태를 저장하고 직접 조작하는 **변수 중심의 프로그래밍**입니다.